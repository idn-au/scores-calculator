# YAML to Markdown
This is a small Python script to generate a series of nested HTML tables for rendering in Markdown to display the scoring rules as defined in the YAML files in the [`/definitions`](/definitions) directory.

## Install & Run
To install the project (requires uv), run:

```bash
uv sync
```

To run:

```bash
uv run main.py
```
