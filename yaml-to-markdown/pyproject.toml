[project]
name = "yaml-to-markdown"
version = "0.1.0"
authors = [
    {name = "jamiefeiss", email = "jamie.feiss@gmail.com"}
]
description = "A script to convert YAML score definitions to Markdown"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0.3",
]
